#!/usr/bin/env node
const selectResource = require('./lib/selectContainer');

selectResource.run((result) => {
    console.log();    
    console.log(`${result.container.name} selected.`);
    console.log();
});
