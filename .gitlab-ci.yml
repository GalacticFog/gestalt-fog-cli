image:
  name: galacticfog/docker-yarn-debian:8.11.3

stages:
  - build
  - test
  - deploy

build:
  stage: build
  allow_failure: false
  script:
   - apt-get update
   - apt-get install zip unzip
   - apt-get install pkg 
   - ./build.sh
